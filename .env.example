# Frontend env vars (Vite — must be prefixed VITE_)
# For local dev: use localhost
# For mobile APK build: use your laptop IP (e.g. http://10.65.220.24:3000)
# For production (Render): set to your deployed service URLs
VITE_API_BASE_URL=http://localhost:3000
VITE_ML_BASE_URL=http://localhost:8000
